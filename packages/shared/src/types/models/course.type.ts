type Course = {
  // course_id: number;
  // user_id: number;
  // category_id: number;
  title: string;
  description: string;
  image_url: string;
  price: number;
  isPublished: boolean;
};

export default Course;
